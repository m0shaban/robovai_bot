# ==============================
# RoboVAI - Environment Template
# Copy to .env and fill values
# ==============================

# --- Database (required) ---
# Format: postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/robovai

# --- CORS (required for widget embeds) ---
# Comma-separated origins OR '*' for all (note: '*' disables credentials)
CORS_ALLOW_ORIGINS=*

# --- LLM (optional, but required for AI fallback) ---
# OpenAI-compatible base URL (Groq example below)
LLM_BASE_URL=https://api.groq.com/openai/v1
# Use ONE of these keys:
LLM_API_KEY=
GROQ_API_KEY=
# Default model name
LLM_MODEL=llama-3.1-70b-versatile

# --- Security / Admin (recommended) ---
# Used for signing/crypto in future auth flows
SECRET_KEY=change-me
# Placeholder for future admin auth (not enforced by current endpoints)
ADMIN_PASSWORD=

# --- Webhooks / Lead capture ---
# HTTP timeout (seconds) for outbound lead webhook calls
WEBHOOK_TIMEOUT=5

# --- Streamlit dashboard ---
# Used when running dashboard inside Docker
BACKEND_URL=http://localhost:8000
