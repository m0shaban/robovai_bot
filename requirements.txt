# Core API
fastapi>=0.115.0,<0.116
uvicorn[standard]>=0.30.0,<0.31
python-multipart>=0.0.9,<1.0

# Settings / Validation (Pydantic v2)
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0.1,<2.0
email-validator>=2.0.0  # Required for EmailStr in Pydantic

# Database (PostgreSQL) - Async SQLAlchemy
SQLAlchemy>=2.0.30,<2.1
asyncpg>=0.29.0,<1.0
alembic>=1.13.0,<2.0

# Auth & Security (multi-tenant SaaS typically needs this)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# HTTP clients
httpx>=0.27.0,<1.0
requests>=2.32.0,<3.0

# Hybrid Response Engine helpers
regex>=2024.5.15

# AI Integration
langchain>=0.3.0,<0.4
langchain-community>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4

# Optional direct Groq client (use if you call Groq without LangChain)
groq>=0.9.0,<1.0

# Observability / Logging
structlog>=24.2.0

# Dev / Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0

# Admin dashboard
streamlit>=1.37.0,<2.0
pandas>=2.2.0,<3.0

# Lint/format (optional but recommended)
ruff>=0.6.0
